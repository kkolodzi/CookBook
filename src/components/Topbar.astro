---
const { user } = Astro.locals;
---

<div
  class="mx-auto mb-4 flex w-full max-w-4xl items-center justify-between rounded-xl border border-white/10 bg-white/5 px-4 py-2 text-sm text-white/80"
>
  {
    user ? (
      <>
        <a href="/recipes" class="font-semibold text-white transition-colors hover:text-purple-200">
          SnapRecipe
        </a>
        <div class="flex items-center gap-3">
          <span class="text-blue-100/70">{user.email}</span>
          <form method="POST" action="/api/auth/signout">
            <button type="submit" class="text-purple-300 transition-colors hover:text-purple-100 hover:underline">
              Sign out
            </button>
          </form>
        </div>
      </>
    ) : (
      <>
        <a href="/" class="font-semibold text-white transition-colors hover:text-purple-200">
          SnapRecipe
        </a>
        <div class="flex gap-3">
          <a href="/auth/signin" class="text-purple-300 transition-colors hover:text-purple-100 hover:underline">
            Sign in
          </a>
          <a href="/auth/signup" class="text-purple-300 transition-colors hover:text-purple-100 hover:underline">
            Sign up
          </a>
        </div>
      </>
    )
  }
</div>
